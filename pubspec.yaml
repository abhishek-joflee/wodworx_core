name: wodworx_core
description: A Flutter package to support the wodworx applications.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.19.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: 0.18.0
  modal_bottom_sheet: 3.0.0-pre

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
